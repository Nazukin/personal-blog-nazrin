<div class="content">
    <!-- Taruh isi konten dari articles json dengan struktur judul, tanggal, isi -->
    <h1><%= article.title %></h1>
    <h3><%= new Date(article.date).toLocaleDateString('id-ID',{day: 'numeric',month: 'long', year: 'numeric'}) %></h3>
    <p><%= article.content %></p>
</div>