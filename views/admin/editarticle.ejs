<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Aricle</title>
    <link rel="stylesheet" href="/styles/home.css">
</head>
<body>
    <%- include('../partials/header',{hideAddButton: false, admin: true}) %>
    <div class="content-add">
        <form action="/admin/editarticle/<%= article.id %>"" method="post">
            <input type="hidden" name="id" value="<%= article.id %>">
            <label for="title">Title</label>
            <br>
            <input type="text" name="title" id="title" value="<%= article.title %>" required>
            <br>
            <label for="content">Content</label>
            <br>
            <textarea name="content" id="content" rows="10" required><%= article.content %></textarea>
            <br>
            <input type="submit" value="Submit">
        </form>
</body>
</html>